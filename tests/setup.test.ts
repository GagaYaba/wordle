import { describe, expect, it } from 'vitest';

describe('Test environment', () => {
  it('Given a working test environment, When Vitest runs, Then the setup test should pass', () => {
    expect(true).toBe(true);
  });
});
